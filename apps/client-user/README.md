# Client App

React frontend for the SOC registration and assignment workflow.

## Environment

Create `.env` from `.env.example`:

```bash
VITE_API_URL=http://localhost:5001/api
```

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`
